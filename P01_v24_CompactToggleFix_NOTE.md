# P01 v24 Compact Toggle Fix Note

本版延續 v23，不更動 SQL 與 Supabase Edge Functions，只修正前端介面狀態。

## 修正項目

1. 比賽開始後，上方競賽資訊會自動收合一次。
2. 因系統會定期輪詢更新競賽狀態，v23 會在每次更新時重新把資訊區收合，造成使用者按「展開」後很快又被關閉。
3. v24 改為「自動收合只執行一次」，之後尊重使用者手動展開／收合狀態。
4. 等待階段的競賽資訊也只在第一次進入時自動展開，避免輪詢覆蓋使用者操作。

## 影響範圍

- 修改檔案：`app.js`
- 不需重新執行 SQL
- 不需重新部署 Edge Functions
- 只需重新上傳 GitHub Pages 前端檔案
